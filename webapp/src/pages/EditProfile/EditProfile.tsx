import React, { FC } from "react";
import s from "./EditProfile.module.scss";

const EditProfile: FC = () => {
  return <div>Waiting for model implementations...</div>;
};

export default EditProfile;
